import { createRoot } from 'react-dom/client';

// const root = createRoot(document.body);
// root.render(
//     <h2>Hello from React!</h2>
// );

function App() {
    return(<h1>this is some content</h1>)
}

export default App